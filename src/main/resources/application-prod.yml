spring:
  application:
    name: bookstorebackend

  datasource:
    url: jdbc:mysql://arn:aws:rds:ap-southeast-2:579884119149:db:bookstore-ecommerce-db:3306/bookstoreecommerceproddb
    username: admin
    password: "12345678"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: replace-with-secure-secret
    access-expiration-seconds: 3600

logging:
  level:
    org:
      springframework:
        security: DEBUG
