# SEQ-008b: Approve/Reject Promotion

> **Sequence ID:** SEQ-008b
> **Maps to:** UC-008b
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Approve Promotion

```mermaid
sequenceDiagram
    autonumber
    actor AD as 👤 Admin
    participant PC as 🏷️ PromotionController
    participant PS as 🏷️ PromotionService
    participant PR as 🗄️ PromotionRepository
    participant DB as 💾 MySQL DB

    AD->>PC: PATCH /api/promotions/{id}/approve
    PC->>PS: approvePromotion(id)
    PS->>DB: SELECT * FROM promotions WHERE id = ?
    DB-->>PS: Promotion
    PS->>DB: UPDATE promotions SET status='ACTIVE' WHERE id = ?
    PS-->>PC: PromotionResponse
    PC-->>AD: 200 OK + PromotionResponse
```

---

## 2. Reject Promotion

```mermaid
sequenceDiagram
    autonumber
    actor AD as 👤 Admin
    participant PC as 🏷️ PromotionController
    participant PS as 🏷️ PromotionService
    participant DB as 💾 MySQL DB

    AD->>PC: PATCH /api/promotions/{id}/reject
    PC->>PS: rejectPromotion(id)
    PS->>DB: UPDATE promotions SET status='REJECTED' WHERE id = ?
    PS-->>PC: PromotionResponse
    PC-->>AD: 200 OK + PromotionResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
