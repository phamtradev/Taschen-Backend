# SEQ-006d: Sync Stock

> **Sequence ID:** SEQ-006d
> **Maps to:** UC-006d
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Sync Stock to Book

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant BC as 📦 BatchController
    participant BS as 📦 BatchService
    participant BR as 🗄️ BatchRepository
    participant DB as 💾 MySQL DB

    WS->>BC: PUT /api/batches/book/{bookId}/sync-stock
    BC->>BS: syncStockToBook(bookId)
    BS->>BR: sumRemainingQuantityByBookId(bookId)
    BR->>DB: SELECT SUM(remaining_quantity) FROM batches WHERE book_id = ?
    DB-->>BR: totalStock (e.g., 150)
    BR-->>BS: totalStock
    BS->>DB: UPDATE books SET stock_quantity = 150 WHERE id = bookId
    BS-->>BC: totalStock (150)
    BC-->>WS: 200 OK + { "totalStock": 150 }
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
