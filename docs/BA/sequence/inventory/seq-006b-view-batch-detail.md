# SEQ-006b: View Batch Detail

> **Sequence ID:** SEQ-006b
> **Maps to:** UC-006b
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. View Batch Detail

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant BC as 📦 BatchController
    participant BS as 📦 BatchService
    participant BR as 🗄️ BatchRepository
    participant DB as 💾 MySQL DB

    WS->>BC: GET /api/batches/{batchId}
    BC->>BS: getBatchById(batchId)
    BS->>BR: findById(batchId)
    BR->>DB: SELECT * FROM batches WHERE id = ?
    DB-->>BR: Batch
    alt Batch exists
        BR-->>BS: Batch
        BS-->>BC: BatchResponse
        BC-->>WS: 200 OK + BatchResponse
    else Batch not found
        BR-->>BS: null
        BS-->>BC: IdInvalidException
        BC-->>WS: 404 Not Found
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
