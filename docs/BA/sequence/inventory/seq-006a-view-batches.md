# SEQ-006a: View Batches

> **Sequence ID:** SEQ-006a
> **Maps to:** UC-006a
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. View All Batches

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant BC as 📦 BatchController
    participant BS as 📦 BatchService
    participant BR as 🗄️ BatchRepository
    participant DB as 💾 MySQL DB

    WS->>BC: GET /api/batches
    BC->>BS: getAllBatches()
    BS->>BR: findAll()
    BR->>DB: SELECT * FROM batches ORDER BY created_at DESC
    DB-->>BR: List<Batch>
    BR-->>BS: List<Batch>
    BS-->>BC: List<BatchResponse>
    BC-->>WS: 200 OK + List<BatchResponse>
```

---

## 2. View Batches by Book

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant BC as 📦 BatchController
    participant BS as 📦 BatchService
    participant BR as 🗄️ BatchRepository
    participant DB as 💾 MySQL DB

    WS->>BC: GET /api/batches/book/{bookId}
    BC->>BS: getBatchesByBookId(bookId)
    BS->>BR: findByBookId(bookId)
    BR->>DB: SELECT * FROM batches WHERE book_id = ? ORDER BY created_at ASC
    DB-->>BR: List<Batch>
    BR-->>BS: List<Batch>
    BS-->>BC: List<BatchResponse>
    BC-->>WS: 200 OK + List<BatchResponse>
```

---

## 3. Get Available Batches (FIFO)

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant BC as 📦 BatchController
    participant BS as 📦 BatchService
    participant BR as 🗄️ BatchRepository
    participant DB as 💾 MySQL DB

    WS->>BC: GET /api/batches/book/{bookId}/available
    BC->>BS: getAvailableBatches(bookId)
    BS->>BR: findAvailableBatchesByBookId(bookId)
    BR->>DB: SELECT * FROM batches WHERE book_id = ? AND remaining_quantity > 0 ORDER BY created_at ASC
    DB-->>BR: List<Batch>
    BR-->>BS: List<Batch>
    BS-->>BC: List<BatchResponse>
    BC-->>WS: 200 OK + List<BatchResponse>
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
