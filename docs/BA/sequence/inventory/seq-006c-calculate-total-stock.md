# SEQ-006c: Calculate Total Stock

> **Sequence ID:** SEQ-006c
> **Maps to:** UC-006c
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Calculate Total Stock

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant BC as 📦 BatchController
    participant BS as 📦 BatchService
    participant BR as 🗄️ BatchRepository
    participant DB as 💾 MySQL DB

    WS->>BC: GET /api/batches/book/{bookId}/total-stock
    BC->>BS: calculateTotalStock(bookId)
    BS->>BR: sumRemainingQuantityByBookId(bookId)
    BR->>DB: SELECT SUM(remaining_quantity) FROM batches WHERE book_id = ? AND remaining_quantity > 0
    DB-->>BR: totalStock (e.g., 150)
    BR-->>BS: totalStock
    BS-->>BC: totalStock
    BC-->>WS: 200 OK + { "totalStock": 150 }
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
