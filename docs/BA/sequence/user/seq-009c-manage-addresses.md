# SEQ-009c: Manage Addresses

> **Sequence ID:** SEQ-009c
> **Maps to:** UC-009c
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Add Address

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 📍 AddressController
    participant AS as 📍 AddressService
    participant AR as 🗄️ AddressRepository
    participant DB as 💾 MySQL DB

    U->>AC: POST /api/users/{userId}/addresses
    AC->>AS: createAddress(userId, request)
    AS->>AR: save(address)
    AR->>DB: INSERT INTO addresses (...)
    DB-->>AR: Address
    AS-->>AC: AddressResponse
    AC-->>U: 201 Created + AddressResponse
```

---

## 2. Set Default Address

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 📍 AddressController
    participant AS as 📍 AddressService
    participant AR as 🗄️ AddressRepository
    participant DB as 💾 MySQL DB

    U->>AC: PUT /api/users/{userId}/addresses/{id}/default
    AC->>AS: setDefaultAddress(userId, addressId)
    AS->>AR: findAllByUserId(userId)
    AR->>DB: SELECT * FROM addresses WHERE user_id = ?
    DB-->>AR: List<Address>
    AR-->>AS: List<Address>
    loop For each address
        AS->>DB: UPDATE addresses SET is_default = false WHERE user_id = ?
    end
    AS->>DB: UPDATE addresses SET is_default = true WHERE id = ?
    AS-->>AC: AddressResponse
    AC-->>U: 200 OK + AddressResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
