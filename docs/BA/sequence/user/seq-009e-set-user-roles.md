# SEQ-009e: Set User Roles

> **Sequence ID:** SEQ-009e
> **Maps to:** UC-009e
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Set User Roles

```mermaid
sequenceDiagram
    autonumber
    actor AD as 👤 Admin
    participant UC as 👤 UserController
    participant US as 👤 UserService
    participant UR as 🗄️ UserRepository
    participant RR as 🗄️ RoleRepository
    participant DB as 💾 MySQL DB

    AD->>UC: PATCH /api/users/{id}/roles/codes
    UC->>US: setUserRoles(userId, roleCodes)
    US->>UR: findById(userId)
    UR->>DB: SELECT * FROM users WHERE id = ?
    DB-->>UR: User
    UR-->>US: User
    US->>RR: findByCodeIn(roleCodes)
    RR->>DB: SELECT * FROM roles WHERE code IN (?)
    DB-->>RR: List<Role>
    RR-->>US: List<Role>
    US->>US: user.roles = newRoles
    US->>UR: save(user)
    UR->>DB: UPDATE users SET ... WHERE id = ?
    US-->>UC: UserResponse
    UC-->>AD: 200 OK + UserResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
