# SEQ-006: Stock Replenishment

> **Sequence ID:** SEQ-006
> **Maps to:** UC-006 / UC-010
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## Stock Replenishment Full Flow

```mermaid
sequenceDiagram
    autonumber
    actor SE as 👤 Seller
    actor WS as 👤 Warehouse Staff
    participant SRC as 📋 StockRequestController
    participant SRS as 📋 StockRequestService
    participant POC as 🛒 PurchaseOrderController
    participant POS as 🛒 PurchaseOrderService
    participant DB as 💾 MySQL DB
    participant NS as 🔔 NotificationService

    Note over SE,DB: === STEP 1: Create Stock Request ===
    SE->>SRC: POST /api/stock-requests
    SRS->>DB: INSERT INTO stock_requests (status=PENDING)
    SRS->>NS: notifyWarehouseStaff("New stock request")
    NS-->>WS: 🔔 New stock request!
    SRS-->>SE: StockRequestResponse

    Note over SE,DB: === STEP 2: Approve Stock Request ===
    WS->>SRC: PUT /api/stock-requests/{id}/approve
    SRS->>DB: UPDATE stock_requests SET status='APPROVED'
    SRS->>NS: notifySeller("Stock request approved")
    NS-->>SE: 🔔 Approved!
    SRS-->>WS: StockRequestResponse

    Note over SE,DB: === STEP 3: Create Purchase Order ===
    WS->>POC: POST /api/purchase-orders/from-stock-request
    POS->>DB: INSERT INTO purchase_orders (status=PENDING)
    POS->>DB: INSERT INTO purchase_order_items
    POS->>NS: notifyStaff("PO created")
    POS-->>WS: PurchaseOrderResponse

    Note over SE,DB: === STEP 4: Approve & Pay Purchase Order ===
    WS->>POC: PUT /api/purchase-orders/{id}/approve
    POS->>DB: UPDATE purchase_orders SET status='APPROVED'
    WS->>POC: POST /api/purchase-orders/{id}/pay
    POS->>DB: UPDATE purchase_orders SET status='ORDERED'
    POS->>NS: notifySupplier("Order placed")
    POS-->>WS: PurchaseOrderResponse

    Note over SE,DB: === STEP 5: Supplier Delivers ===
    Note over WS: Supplier delivers goods to warehouse
    Note over WS: WS creates ImportStock
```

---

## Stock Request Create & Approve

```mermaid
sequenceDiagram
    autonumber
    actor SE as 👤 Seller
    participant SRC as 📋 StockRequestController
    participant SRS as 📋 StockRequestService
    participant SRR as 🗄️ StockRequestRepository
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    SE->>SRC: POST /api/stock-requests
    SRC->>SRS: createStockRequest(request)
    SRS->>BR: findById(bookId)
    BR->>DB: SELECT * FROM books WHERE id = ?
    DB-->>BR: Book
    BR-->>SRS: Book
    SRS->>SRR: save(stockRequest)
    SRR->>DB: INSERT INTO stock_requests (status=PENDING)
    DB-->>SRR: StockRequest
    SRR-->>SRS: StockRequest
    SRS-->>SRC: StockRequestResponse
    SRC-->>SE: 201 Created + StockRequestResponse

    actor WS as 👤 Warehouse Staff
    WS->>SRC: PUT /api/stock-requests/{id}/approve
    SRC->>SRS: approveStockRequest(id, request)
    SRS->>SRR: findById(id)
    SRR->>DB: SELECT * FROM stock_requests WHERE id = ?
    DB-->>SRR: StockRequest
    SRR-->>SRS: StockRequest
    SRS->>DB: UPDATE stock_requests SET status='APPROVED', processed_at=NOW()
    SRS-->>SRC: StockRequestResponse
    SRC-->>WS: 200 OK + StockRequestResponse
```

---

## Purchase Order Create & Approve

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 Warehouse Staff
    participant POC as 🛒 PurchaseOrderController
    participant POS as 🛒 PurchaseOrderService
    participant POR as 🗄️ PurchaseOrderRepository
    participant DB as 💾 MySQL DB

    WS->>POC: POST /api/purchase-orders
    POC->>POS: createPurchaseOrder(request)
    POS->>DB: INSERT INTO purchase_orders (status=PENDING)
    POS->>DB: INSERT INTO purchase_order_items
    POS-->>POC: PurchaseOrderResponse
    POC-->>WS: 201 Created + PurchaseOrderResponse

    WS->>POC: PUT /api/purchase-orders/{id}/approve
    POC->>POS: approvePurchaseOrder(id, approvedById)
    POS->>DB: SELECT * FROM purchase_orders WHERE id = ?
    DB-->>POS: PurchaseOrder
    POS->>DB: UPDATE purchase_orders SET status='APPROVED', approved_at=NOW()
    POS-->>POC: PurchaseOrderResponse
    POC-->>WS: 200 OK + PurchaseOrderResponse

    WS->>POC: POST /api/purchase-orders/{id}/pay
    POC->>POS: payPurchaseOrder(id, paidById)
    POS->>DB: UPDATE purchase_orders SET status='ORDERED'
    POS-->>POC: PurchaseOrderResponse
    POC-->>WS: 200 OK + PurchaseOrderResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
