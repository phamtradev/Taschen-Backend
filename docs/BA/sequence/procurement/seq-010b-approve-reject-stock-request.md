# SEQ-010b: Approve/Reject Stock Request

> **Sequence ID:** SEQ-010b
> **Maps to:** UC-010b
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Approve Stock Request

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 Warehouse Staff
    participant SRC as 📋 StockRequestController
    participant SRS as 📋 StockRequestService
    participant SRR as 🗄️ StockRequestRepository
    participant DB as 💾 MySQL DB

    WS->>SRC: PUT /api/stock-requests/{id}/approve
    SRC->>SRS: approveStockRequest(id)
    SRS->>SRR: findById(id)
    SRR->>DB: SELECT * FROM stock_requests WHERE id = ?
    DB-->>SRR: StockRequest
    SRR-->>SRS: StockRequest
    SRS->>DB: UPDATE stock_requests SET status='APPROVED', processed_at=NOW()
    SRS-->>SRC: StockRequestResponse
    SRC-->>WS: 200 OK + StockRequestResponse
```

---

## 2. Reject Stock Request

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 Warehouse Staff
    participant SRC as 📋 StockRequestController
    participant SRS as 📋 StockRequestService
    participant DB as 💾 MySQL DB

    WS->>SRC: PUT /api/stock-requests/{id}/reject
    SRC->>SRS: rejectStockRequest(id)
    SRS->>DB: UPDATE stock_requests SET status='REJECTED'
    SRS-->>SRC: StockRequestResponse
    SRC-->>WS: 200 OK + StockRequestResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
