# SEQ-010c: Create Purchase Order

> **Sequence ID:** SEQ-010c
> **Maps to:** UC-010c
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Create Purchase Order

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 Warehouse Staff
    participant POC as 🛒 PurchaseOrderController
    participant POS as 🛒 PurchaseOrderService
    participant POR as 🗄️ PurchaseOrderRepository
    participant DB as 💾 MySQL DB

    WS->>POC: POST /api/purchase-orders
    POC->>POS: createPurchaseOrder(request)
    POS->>DB: INSERT INTO purchase_orders (status=PENDING)
    POS->>DB: INSERT INTO purchase_order_items
    POS-->>POC: PurchaseOrderResponse
    POC-->>WS: 201 Created + PurchaseOrderResponse
```

---

## 2. Create from Stock Request

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 Warehouse Staff
    participant POC as 🛒 PurchaseOrderController
    participant POS as 🛒 PurchaseOrderService
    participant SRR as 🗄️ StockRequestRepository
    participant DB as 💾 MySQL DB

    WS->>POC: POST /api/purchase-orders/from-stock-request
    POC->>POS: createFromStockRequest(request)
    POS->>SRR: findById(stockRequestId)
    SRR->>DB: SELECT * FROM stock_requests WHERE id = ? AND status='APPROVED'
    DB-->>SRR: StockRequest
    SRR-->>POS: StockRequest
    POS->>DB: INSERT INTO purchase_orders (status=PENDING)
    POS->>DB: INSERT INTO purchase_order_items (from StockRequest)
    POS-->>POC: PurchaseOrderResponse
    POC-->>WS: 201 Created + PurchaseOrderResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
