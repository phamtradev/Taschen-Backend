# SEQ-010d: Approve & Pay Purchase Order

> **Sequence ID:** SEQ-010d
> **Maps to:** UC-010d
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Approve Purchase Order

```mermaid
sequenceDiagram
    autonumber
    actor WS as 👤 WS/Admin
    participant POC as 🛒 PurchaseOrderController
    participant POS as 🛒 PurchaseOrderService
    participant DB as 💾 MySQL DB

    WS->>POC: PUT /api/purchase-orders/{id}/approve
    POC->>POS: approvePurchaseOrder(id)
    POS->>DB: UPDATE purchase_orders SET status='APPROVED', approved_at=NOW()
    POS-->>POC: PurchaseOrderResponse
    POC-->>WS: 200 OK + PurchaseOrderResponse
```

---

## 2. Pay Purchase Order

```mermaid
sequenceDiagram
    autonumber
    actor AD as 👤 Admin
    participant POC as 🛒 PurchaseOrderController
    participant POS as 🛒 PurchaseOrderService
    participant DB as 💾 MySQL DB

    AD->>POC: POST /api/purchase-orders/{id}/pay
    POC->>POS: payPurchaseOrder(id, paidById)
    POS->>DB: UPDATE purchase_orders SET status='ORDERED', paid_at=NOW()
    POS-->>POC: PurchaseOrderResponse
    POC-->>AD: 200 OK + PurchaseOrderResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
