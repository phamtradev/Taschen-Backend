# SEQ-003c: Update Quantity

> **Sequence ID:** SEQ-003c
> **Maps to:** UC-003c
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Increase Quantity

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CIC as 🛒 CartItemController
    participant CIS as 🛒 CartItemService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CIC: PATCH /api/cart-items/{cartItemId}/increase
    CIC->>CIS: increaseQuantity(cartItemId)
    CIS->>CIR: findById(cartItemId)
    CIR->>DB: SELECT * FROM cart_items WHERE id = ?
    DB-->>CIR: CartItem
    CIR-->>CIS: CartItem
    CIS->>DB: UPDATE cart_items SET quantity = quantity + 1 WHERE id = ?
    CIS->>CIS: Recalculate cart.totalPrice
    CIS->>CR: save(cart)
    CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
    CIS-->>CIC: CartResponse
    CIC-->>U: 200 OK + CartResponse
```

---

## 2. Decrease Quantity

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CIC as 🛒 CartItemController
    participant CIS as 🛒 CartItemService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CIC: PATCH /api/cart-items/{cartItemId}/decrease
    CIC->>CIS: decreaseQuantity(cartItemId)
    CIS->>CIR: findById(cartItemId)
    CIR->>DB: SELECT * FROM cart_items WHERE id = ?
    DB-->>CIR: CartItem
    CIR-->>CIS: CartItem
    alt quantity > 1
        CIS->>DB: UPDATE cart_items SET quantity = quantity - 1 WHERE id = ?
        CIS->>CIS: Recalculate cart.totalPrice
        CIS->>CR: save(cart)
        CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
        CIS-->>CIC: CartResponse
        CIC-->>U: 200 OK + CartResponse
    else quantity == 1
        CIS->>CIR: delete(cartItem)
        CIR->>DB: DELETE FROM cart_items WHERE id = ?
        CIS->>CIS: Recalculate cart.totalPrice
        CIS->>CR: save(cart)
        CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
        CIS-->>CIC: CartResponse (item removed)
        CIC-->>U: 200 OK + CartResponse
    end
```

---

## 3. Set Quantity (Direct Update)

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CIC as 🛒 CartItemController
    participant CIS as 🛒 CartItemService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CIC: PUT /api/cart-items/{cartItemId}/quantity
    CIC->>CIS: updateQuantity(cartItemId, newQuantity)
    CIS->>CIR: findById(cartItemId)
    CIR->>DB: SELECT * FROM cart_items WHERE id = ?
    DB-->>CIR: CartItem
    CIR-->>CIS: CartItem
    alt newQuantity > 0
        CIS->>DB: UPDATE cart_items SET quantity = ? WHERE id = ?
        CIS->>CIS: Recalculate cart.totalPrice
        CIS->>CR: save(cart)
        CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
        CIS-->>CIC: CartResponse
        CIC-->>U: 200 OK + CartResponse
    else newQuantity == 0
        CIS->>CIR: delete(cartItem)
        CIR->>DB: DELETE FROM cart_items WHERE id = ?
        CIS->>CIS: Recalculate cart.totalPrice
        CIS->>CR: save(cart)
        CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
        CIS-->>CIC: CartResponse (item removed)
        CIC-->>U: 200 OK + CartResponse
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
