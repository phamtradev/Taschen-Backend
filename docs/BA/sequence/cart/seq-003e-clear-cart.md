# SEQ-003e: Clear Cart

> **Sequence ID:** SEQ-003e
> **Maps to:** UC-003e
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Clear Cart

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CC as 🛒 CartController
    participant CS as 🛒 CartService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CC: DELETE /api/carts/users/{userId}
    CC->>CS: clearCart(userId)
    CS->>CR: findByUserId(userId)
    CR->>DB: SELECT * FROM carts WHERE user_id = ?
    DB-->>CR: Cart
    CR-->>CS: Cart
    CS->>CIR: deleteAllByCartId(cart.id)
    CIR->>DB: DELETE FROM cart_items WHERE cart_id = ?
    CS->>CS: cart.totalPrice = 0
    CS->>CR: save(cart)
    CR->>DB: UPDATE carts SET total_price = 0 WHERE id = ?
    CS-->>CC: void
    CC-->>U: 204 No Content
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
