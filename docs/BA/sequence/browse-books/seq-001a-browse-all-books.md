# SEQ-001a: Browse All Books

> **Sequence ID:** SEQ-001a
> **Maps to:** UC-001a
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Browse All Books (Paginated)

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books?page=1&size=10&sortBy=id&sortDir=asc
    BC->>BS: getAllBooks(Pageable)
    BS->>BR: findAll(Pageable)
    BR->>DB: SELECT * FROM books WHERE is_active = true ORDER BY id ASC LIMIT 10 OFFSET 0
    DB-->>BR: List<Book>
    BR-->>BS: Page<Book>
    BS-->>BC: PageResponse<BookResponse>
    BC-->>G: 200 OK + PageResponse<BookResponse>
```

---

## 2. Browse All Books (Sorted)

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/sorted?sortByField=price&sortDirection=desc
    BC->>BS: getAllBooksSorted(sortByField, sortDirection)
    BS->>BR: findAllSorted(sortByField, sortDirection)
    BR->>DB: SELECT * FROM books WHERE is_active = true ORDER BY price DESC
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: List<BookResponse>
    BC-->>G: 200 OK + List<BookResponse>
```

---

## 3. Browse All Books Without Pagination

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/all
    BC->>BS: getAllBooks()
    BS->>BR: findAll()
    BR->>DB: SELECT * FROM books WHERE is_active = true
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: List<BookResponse>
    BC-->>G: 200 OK + List<BookResponse>
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
