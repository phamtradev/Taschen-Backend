# SEQ-001b: Search Books

> **Sequence ID:** SEQ-001b
> **Maps to:** UC-001b
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Search Books by Keyword

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/search?keyword=clean+code
    BC->>BS: searchBooks(keyword, pageable)
    BS->>BR: findByTitleContainingIgnoreCaseOrAuthorContainingIgnoreCase(keyword)
    BR->>DB: SELECT * FROM books WHERE (title ILIKE '%clean code%' OR author ILIKE '%clean code%') AND is_active = true
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: PageResponse<BookResponse>
    BC-->>G: 200 OK + PageResponse<BookResponse>
```

---

## 2. Search Books by Category

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/search?categoryId=1
    BC->>BS: searchBooksByCategory(categoryId, pageable)
    BS->>BR: findByCategoryId(categoryId)
    BR->>DB: SELECT b.* FROM books b JOIN book_categories bc ON b.id = bc.book_id WHERE bc.category_id = 1 AND b.is_active = true
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: PageResponse<BookResponse>
    BC-->>G: 200 OK + PageResponse<BookResponse>
```

---

## 3. Search Books Combined (Keyword + Category)

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/search?keyword=java&categoryId=2
    BC->>BS: searchBooks(keyword, categoryId, pageable)
    BS->>BR: findByKeywordAndCategoryId(keyword, categoryId)
    BR->>DB: SELECT b.* FROM books b JOIN book_categories bc ON b.id = bc.book_id WHERE bc.category_id = 2 AND (b.title ILIKE '%java%' OR b.author ILIKE '%java%') AND b.is_active = true
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: PageResponse<BookResponse>
    BC-->>G: 200 OK + PageResponse<BookResponse>
```

---

## 4. Search Books by Supplier

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/supplier/{supplierId}
    BC->>BS: getBooksBySupplierId(supplierId)
    BS->>BR: findBySupplierId(supplierId)
    BR->>DB: SELECT * FROM books WHERE supplier_id = ? AND is_active = true
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: List<BookResponse>
    BC-->>G: 200 OK + List<BookResponse>
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
