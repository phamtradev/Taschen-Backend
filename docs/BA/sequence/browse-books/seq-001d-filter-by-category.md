# SEQ-001d: Filter by Category

> **Sequence ID:** SEQ-001d
> **Maps to:** UC-001d
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Browse All Categories

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant CC as 📂 CategoryController
    participant CS as 📂 CategoryService
    participant CR as 🗄️ CategoryRepository
    participant DB as 💾 MySQL DB

    G->>CC: GET /api/categories
    CC->>CS: getAllCategories()
    CS->>CR: findAll()
    CR->>DB: SELECT * FROM categories WHERE is_active = true ORDER BY name ASC
    DB-->>CR: List<Category>
    CR-->>CS: List<Category>
    CS-->>CC: List<CategoryResponse>
    CC-->>G: 200 OK + List<CategoryResponse>
```

---

## 2. Get Books by Category

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/category/{categoryId}
    BC->>BS: getBooksByCategoryId(categoryId)
    BS->>BR: findByCategoryId(categoryId)
    BR->>DB: SELECT b.* FROM books b JOIN book_categories bc ON b.id = bc.book_id WHERE bc.category_id = ? AND b.is_active = true
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>BC: List<BookResponse>
    BC-->>G: 200 OK + List<BookResponse>
```

---

## 3. Browse Variants (Formats)

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant VC as 🏷️ VariantController
    participant VS as 🏷️ VariantService
    participant VR as 🗄️ VariantRepository
    participant DB as 💾 MySQL DB

    G->>VC: GET /api/variants
    VC->>VS: getAllVariants()
    VS->>VR: findAll()
    VR->>DB: SELECT * FROM variants WHERE is_active = true
    DB-->>VR: List<Variant>
    VR-->>VS: List<Variant>
    VS-->>VC: List<VariantResponse>
    VC-->>G: 200 OK + List<VariantResponse>
```

---

## 4. Get Variants by Book

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant VC as 🏷️ VariantController
    participant VS as 🏷️ VariantService
    participant VR as 🗄️ VariantRepository
    participant DB as 💾 MySQL DB

    G->>VC: GET /api/variants/book/{bookId}
    VC->>VS: getVariantsByBookId(bookId)
    VS->>VR: findByBookId(bookId)
    VR->>DB: SELECT * FROM variants WHERE book_id = ? AND is_active = true
    DB-->>VR: List<Variant>
    VR-->>VS: List<Variant>
    VS-->>VC: List<VariantResponse>
    VC-->>G: 200 OK + List<VariantResponse>
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
