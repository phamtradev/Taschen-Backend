# SEQ-001c: View Book Detail

> **Sequence ID:** SEQ-001c
> **Maps to:** UC-001c
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Get Book Detail

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/{bookId}
    BC->>BS: getBookById(bookId)
    BS->>BR: findById(bookId)
    BR->>DB: SELECT * FROM books WHERE id = ? AND is_active = true
    DB-->>BR: Book (or null)
    alt Book exists and active
        BR-->>BS: Book
        BS->>BS: Load categories (EAGER)
        BS->>BS: Load variants (EAGER)
        BS->>BS: Load supplier (EAGER)
        BS-->>BC: BookResponse (full detail)
        BC-->>G: 200 OK + BookResponse
    else Book not found or inactive
        BR-->>BS: null
        BS-->>BC: IdInvalidException("Book not found")
        BC-->>G: 400 Bad Request
    end
```

---

## 2. Get Book Detail with Related Books

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BES as 🤖 BookEmbeddingService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/{bookId}?includeRelated=true
    BC->>BS: getBookByIdWithRelated(bookId)
    BS->>BR: findById(bookId)
    BR->>DB: SELECT * FROM books WHERE id = ?
    DB-->>BR: Book
    BR-->>BS: Book
    BS->>BES: findSimilarBooks(bookId, limit=4)
    BES->>DB: Calculate similarity
    BES-->>BS: List<Book> (similar books)
    BS-->>BC: BookDetailResponse (with related books)
    BC-->>G: 200 OK + BookDetailResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
