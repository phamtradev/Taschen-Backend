# SEQ-001f: Browse Banners

> **Sequence ID:** SEQ-001f
> **Maps to:** UC-001f
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Browse All Active Banners

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 🎨 BannerController
    participant BS as 🎨 BannerService
    participant BR as 🗄️ BannerRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/banners
    BC->>BS: getActiveBanners()
    BS->>BR: findByIsActiveTrue()
    BR->>DB: SELECT * FROM banners WHERE is_active = true AND start_date <= NOW() AND end_date >= NOW() ORDER BY display_order ASC
    DB-->>BR: List<Banner>
    BR-->>BS: List<Banner>
    BS-->>BC: List<BannerResponse>
    BC-->>G: 200 OK + List<BannerResponse>
```

---

## 2. Browse Banners by Position

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 🎨 BannerController
    participant BS as 🎨 BannerService
    participant BR as 🗄️ BannerRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/banners/position/HOMEPAGE
    BC->>BS: getBannersByPosition(position)
    BS->>BR: findByPositionAndIsActiveTrue(position)
    BR->>DB: SELECT * FROM banners WHERE position = 'HOMEPAGE' AND is_active = true AND start_date <= NOW() AND end_date >= NOW() ORDER BY display_order ASC
    DB-->>BR: List<Banner>
    BR-->>BS: List<Banner>
    BS-->>BC: List<BannerResponse>
    BC-->>G: 200 OK + List<BannerResponse>
```

---

## 3. Get Banner Detail

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 🎨 BannerController
    participant BS as 🎨 BannerService
    participant BR as 🗄️ BannerRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/banners/{bannerId}
    BC->>BS: getBannerById(bannerId)
    BS->>BR: findById(bannerId)
    BR->>DB: SELECT * FROM banners WHERE id = ?
    DB-->>BR: Banner (or null)
    alt Banner exists
        BR-->>BS: Banner
        BS-->>BC: BannerResponse
        BC-->>G: 200 OK + BannerResponse
    else Banner not found
        BR-->>BS: null
        BS-->>BC: IdInvalidException("Banner not found")
        BC-->>G: 404 Not Found
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
