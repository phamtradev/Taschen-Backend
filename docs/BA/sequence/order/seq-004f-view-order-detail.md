# SEQ-004f: View Order Detail

> **Sequence ID:** SEQ-004f
> **Maps to:** UC-004f
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. View Order Detail

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant OC as 📦 OrderController
    participant OS as 📦 OrderService
    participant OR as 🗄️ OrderRepository
    participant DB as 💾 MySQL DB

    U->>OC: GET /api/orders/{orderId}
    OC->>OS: getOrderById(orderId)
    OS->>DB: SELECT * FROM orders WHERE id = ? AND user_id = ?
    DB-->>OS: Order
    alt Order exists and belongs to user
        OS->>DB: SELECT od.*, b.title, b.author FROM order_details od JOIN books b ON od.book_id = b.id WHERE od.order_id = ?
        OS->>DB: SELECT a.* FROM addresses a WHERE a.id = (SELECT address_id FROM orders WHERE id = ?)
        OS-->>OC: OrderDetailResponse
        OC-->>U: 200 OK + OrderDetailResponse
    else Order not found or unauthorized
        OS-->>OC: IdInvalidException("Order not found")
        OC-->>U: 404 Not Found
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
