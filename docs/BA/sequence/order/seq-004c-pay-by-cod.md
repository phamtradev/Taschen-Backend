# SEQ-004c: Pay by COD

> **Sequence ID:** SEQ-004c
> **Maps to:** UC-004c
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Pay by COD

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant OC as 📦 OrderController
    participant OS as 📦 OrderService
    participant OR as 🗄️ OrderRepository
    participant DB as 💾 MySQL DB

    U->>OC: PUT /api/orders/{orderId}/pay-cod
    OC->>OS: payByCOD(orderId)
    OS->>DB: SELECT * FROM orders WHERE id = ? AND user_id = ?
    DB-->>OS: Order
    OS->>OS: Check status == UNPAID
    alt Status is UNPAID
        OS->>OS: order.paymentMethod = COD
        OS->>DB: UPDATE orders SET payment_method = 'COD', status = 'PENDING' WHERE id = ?
        OS-->>OC: OrderResponse
        OC-->>U: 200 OK + OrderResponse
    else Status is not UNPAID
        OS-->>OC: IdInvalidException("Order already paid or cancelled")
        OC-->>U: 400 Bad Request
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
