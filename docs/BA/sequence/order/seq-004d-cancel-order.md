# SEQ-004d: Cancel Order

> **Sequence ID:** SEQ-004d
> **Maps to:** UC-004d
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Cancel Order

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant OC as 📦 OrderController
    participant OS as 📦 OrderService
    participant OR as 🗄️ OrderRepository
    participant BS as 📦 BatchService
    participant NS as 🔔 NotificationService
    participant DB as 💾 MySQL DB

    U->>OC: PUT /api/orders/{orderId}/cancel
    OC->>OS: cancelOrderByCustomer(orderId)
    OS->>DB: SELECT * FROM orders WHERE id = ? AND user_id = ?
    DB-->>OS: Order
    OS->>OS: Check status allows cancellation (UNPAID or PENDING)
    alt Status allows cancel
        OS->>DB: Get OrderDetails
        OS->>BS: restoreStock(orderDetails)
        BS->>DB: UPDATE batches SET remaining_quantity = remaining_quantity + qty WHERE id = ?
        OS->>DB: UPDATE orders SET status = 'CANCELLED' WHERE id = ?
        OS->>NS: sendNotification(userId, "Order cancelled")
        NS-->>U: 🔔 Notification
        OS-->>OC: OrderResponse
        OC-->>U: 200 OK + OrderResponse
    else Status does not allow cancel
        OS-->>OC: IdInvalidException("Cannot cancel order in current status")
        OC-->>U: 400 Bad Request
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
