# SEQ-004e: View My Orders

> **Sequence ID:** SEQ-004e
> **Maps to:** UC-004e
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. View My Orders

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant OC as 📦 OrderController
    participant OS as 📦 OrderService
    participant OR as 🗄️ OrderRepository
    participant JS as 📜 JwtService
    participant DB as 💾 MySQL DB

    U->>OC: GET /api/orders/my
    OC->>JS: extractEmailFromToken()
    OC->>OS: getMyOrders(email)
    OS->>OR: findByUserEmail(email)
    OR->>DB: SELECT * FROM orders WHERE user_id = (SELECT id FROM users WHERE email = ?) ORDER BY order_date DESC
    DB-->>OR: List<Order>
    OR-->>OS: List<Order>
    OS-->>OC: List<OrderSummaryResponse>
    OC-->>U: 200 OK + List<OrderSummaryResponse>
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
