# SEQ-003: Add to Cart

> **Sequence ID:** SEQ-003
> **Maps to:** UC-003
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Get Cart (Auto-Create if Not Exists)

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CC as 🛒 CartController
    participant CS as 🛒 CartService
    participant CR as 🗄️ CartRepository
    participant UR as 🗄️ UserRepository
    participant DB as 💾 MySQL DB

    U->>CC: GET /api/carts/current
    CC->>CS: getCartByCurrentUser()
    CS->>UR: findByEmail(email)
    UR->>DB: SELECT * FROM users WHERE email = ?
    DB-->>UR: User
    UR-->>CS: User
    CS->>CR: findByUser(user)
    CR->>DB: SELECT * FROM carts WHERE user_id = ?
    DB-->>CR: Cart (or null)
    alt Cart exists
        CR-->>CS: Cart
    else Cart not exists
        CS->>CS: Create new Cart()
        CS->>CR: save(cart)
        CR->>DB: INSERT INTO carts (user_id) VALUES (?)
        DB-->>CR: Cart (saved)
        CR-->>CS: Cart
    end
    CS-->>CC: CartResponse
    CC-->>U: 200 OK + CartResponse
```

---

## 2. Add Item to Cart

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CC as 🛒 CartController
    participant CS as 🛒 CartService
    participant CIS as 🛒 CartItemService
    participant CR as 🗄️ CartRepository
    participant CIR as 🗄️ CartItemRepository
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    U->>CC: POST /api/carts/users/{userId}/items
    CC->>CS: addToCart(userId, AddToCartRequest)
    CS->>CR: findByUserId(userId)
    CR->>DB: SELECT * FROM carts WHERE user_id = ?
    DB-->>CR: Cart
    CR-->>CS: Cart
    CS->>BR: findById(bookId)
    BR->>DB: SELECT * FROM books WHERE id = ?
    DB-->>BR: Book
    BR-->>CS: Book
    CS->>CIR: findByCartAndBook(cartId, bookId)
    CIR->>DB: SELECT * FROM cart_items WHERE cart_id = ? AND book_id = ?
    DB-->>CIR: CartItem (or null)
    alt Item already in cart
        CIR-->>CS: CartItem
        CS->>CIS: increaseQuantity(cartItem, request.quantity)
        CIS->>DB: UPDATE cart_items SET quantity = quantity + ? WHERE id = ?
        CIS-->>CS: CartItem
    else Item not in cart
        CS->>CIS: createCartItem(cart, book, request)
        CIS->>DB: INSERT INTO cart_items (cart_id, book_id, quantity, unit_price)
        CIS-->>CS: CartItem
    end
    CS->>CS: Recalculate cart.totalPrice
    CS->>CR: save(cart)
    CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
    CS-->>CC: CartResponse
    CC-->>U: 201 Created + CartResponse
```

---

## 3. Increase Quantity

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CIC as 🛒 CartItemController
    participant CIS as 🛒 CartItemService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CIC: PATCH /api/cart-items/{cartItemId}/increase
    CIC->>CIS: increaseQuantity(cartItemId)
    CIS->>CIR: findById(cartItemId)
    CIR->>DB: SELECT * FROM cart_items WHERE id = ?
    DB-->>CIR: CartItem
    CIR-->>CIS: CartItem
    CIS->>DB: UPDATE cart_items SET quantity = quantity + 1 WHERE id = ?
    CIS->>CIS: Recalculate cart.totalPrice
    CIS->>CR: save(cart)
    CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
    CIS-->>CIC: CartResponse
    CIC-->>U: 200 OK + CartResponse
```

---

## 4. Decrease Quantity

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CIC as 🛒 CartItemController
    participant CIS as 🛒 CartItemService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CIC: PATCH /api/cart-items/{cartItemId}/decrease
    CIC->>CIS: decreaseQuantity(cartItemId)
    CIS->>CIR: findById(cartItemId)
    CIR->>DB: SELECT * FROM cart_items WHERE id = ?
    DB-->>CIR: CartItem
    CIR-->>CIS: CartItem
    alt quantity > 1
        CIS->>DB: UPDATE cart_items SET quantity = quantity - 1 WHERE id = ?
        CIS->>CIS: Recalculate cart.totalPrice
        CIS->>CR: save(cart)
        CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
        CIS-->>CIC: CartResponse
        CIC-->>U: 200 OK + CartResponse
    else quantity == 1
        CIS->>CIR: delete(cartItem)
        CIR->>DB: DELETE FROM cart_items WHERE id = ?
        CIS->>CIS: Recalculate cart.totalPrice
        CIS->>CR: save(cart)
        CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
        CIS-->>CIC: CartResponse (item removed)
        CIC-->>U: 200 OK + CartResponse
    end
```

---

## 5. Remove Item

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CIC as 🛒 CartItemController
    participant CIS as 🛒 CartItemService
    participant CIR as 🗄️ CartItemRepository
    participant CR as 🗄️ CartRepository
    participant DB as 💾 MySQL DB

    U->>CIC: DELETE /api/cart-items/{cartItemId}
    CIC->>CIS: removeItem(cartItemId)
    CIS->>CIR: findById(cartItemId)
    CIR->>DB: SELECT * FROM cart_items WHERE id = ?
    DB-->>CIR: CartItem
    CIR-->>CIS: CartItem
    CIS->>CIR: delete(cartItem)
    CIR->>DB: DELETE FROM cart_items WHERE id = ?
    CIS->>CIS: Recalculate cart.totalPrice
    CIS->>CR: save(cart)
    CR->>DB: UPDATE carts SET total_price = ? WHERE id = ?
    CIS-->>CIC: void
    CIC-->>U: 204 No Content
```

---

## 6. Checkout

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant CC as 🛒 CartController
    participant CS as 🛒 CartService
    participant OS as 📦 OrderService
    participant CR as 🗄️ CartRepository
    participant CIR as 🗄️ CartItemRepository
    participant OR as 🗄️ OrderRepository
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    U->>CC: POST /api/carts/users/{userId}/checkout
    CC->>CS: checkout(userId)
    CS->>CR: findByUserId(userId)
    CR->>DB: SELECT * FROM carts WHERE user_id = ?
    DB-->>CR: Cart
    CR-->>CS: Cart
    CS->>CIR: findByCartId(cart.id)
    CIR->>DB: SELECT * FROM cart_items WHERE cart_id = ?
    DB-->>CIR: List<CartItem>
    CIR-->>CS: List<CartItem>
    alt Cart has items
        CS->>OS: createOrderFromCart(cart, userId)
        OS->>OR: Create Order entity (status = UNPAID)
        OR->>DB: INSERT INTO orders (...)
        OS->>DB: Create OrderDetails for each CartItem
        OS->>CIR: deleteAll(cartItems)
        CIR->>DB: DELETE FROM cart_items WHERE cart_id = ?
        OS->>CR: Update cart.totalPrice = 0
        CR->>DB: UPDATE carts SET total_price = 0 WHERE id = ?
        OS-->>CS: OrderResponse
        CS-->>CC: CartResponse (with new order)
        CC-->>U: 200 OK + OrderResponse
    else Cart is empty
        CS-->>CC: IdInvalidException("Cart is empty")
        CC-->>U: 400 Bad Request
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
