# SEQ-002f: Change Password

> **Sequence ID:** SEQ-002f
> **Maps to:** UC-002f
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Change Password - Success

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant UR as 🗄️ UserRepository
    participant JS as 📜 JwtService
    participant DB as 💾 MySQL DB

    U->>AC: POST /api/auth/change-password
    AC->>AS: changePassword(ChangePasswordRequest)
    AS->>JS: extractUserFromToken()
    AS->>UR: findByEmail(email)
    UR->>DB: SELECT * FROM users WHERE email = ?
    DB-->>UR: User
    UR-->>AS: User
    AS->>AS: Verify oldPassword with BCrypt
    alt Old password correct
        AS->>AS: Hash newPassword
        AS->>UR: save(user)
        UR->>DB: UPDATE users SET password = ? WHERE id = ?
        AS-->>AC: void
        AC-->>U: 204 No Content
    else Old password incorrect
        AS-->>AC: IdInvalidException("Invalid old password")
        AC-->>U: 400 Bad Request
    end
```

---

## 2. Change Password - New Password Same as Old

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant UR as 🗄️ UserRepository
    participant DB as 💾 MySQL DB

    U->>AC: POST /api/auth/change-password
    AC->>AS: changePassword(ChangePasswordRequest)
    AS->>UR: findByEmail(email)
    UR->>DB: SELECT * FROM users WHERE email = ?
    DB-->>UR: User
    UR-->>AS: User
    AS->>AS: Verify oldPassword with BCrypt
    alt Old password correct
        AS->>AS: Check newPassword != oldPassword
        AS-->>AC: IdInvalidException("New password must be different")
        AC-->>U: 400 Bad Request
    end
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
