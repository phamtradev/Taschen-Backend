# SEQ-002b: Verify Email

> **Sequence ID:** SEQ-002b
> **Maps to:** UC-002b
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Verify Email - Success

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant VR as 🗄️ VerificationTokenRepository
    participant UR as 🗄️ UserRepository
    participant DB as 💾 MySQL DB

    U->>AC: GET /api/auth/verify?token=xxx&userId=1
    AC->>AS: verifyEmailTokenForUser(userId, token)
    AS->>VR: findByToken(token)
    VR->>DB: SELECT * FROM verification_tokens WHERE token = ?
    DB-->>VR: VerificationToken
    VR-->>AS: VerificationToken
    AS->>AS: Check token not expired
    AS->>AS: Check token.userId == userId
    alt Token valid
        AS->>UR: findById(userId)
        UR->>DB: SELECT * FROM users WHERE id = ?
        DB-->>UR: User
        UR-->>AS: User
        AS->>AS: user.isActive = true
        AS->>UR: save(user)
        UR->>DB: UPDATE users SET is_active = true WHERE id = ?
        AS->>VR: delete(verificationToken)
        VR->>DB: DELETE FROM verification_tokens WHERE id = ?
        AS-->>AC: void
        AC-->>U: 204 No Content
    else Token invalid/expired
        AS-->>AC: IdInvalidException
        AC-->>U: 400 Bad Request
    end
```

---

## 2. Verify Email - Token Not Found

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant VR as 🗄️ VerificationTokenRepository
    participant DB as 💾 MySQL DB

    U->>AC: GET /api/auth/verify?token=invalid&userId=1
    AC->>AS: verifyEmailTokenForUser(userId, token)
    AS->>VR: findByToken(token)
    VR->>DB: SELECT * FROM verification_tokens WHERE token = ?
    DB-->>VR: null
    VR-->>AS: null
    AS-->>AC: IdInvalidException("Invalid verification token")
    AC-->>U: 400 Bad Request
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
