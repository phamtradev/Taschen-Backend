# SEQ-002e: Logout

> **Sequence ID:** SEQ-002e
> **Maps to:** UC-002e
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Logout - Success

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant RTR as 🗄️ RefreshTokenRepository
    participant DB as 💾 MySQL DB

    U->>AC: POST /api/auth/logout
    AC->>AS: logout(refreshToken)
    AS->>RTR: findByToken(refreshToken)
    RTR->>DB: SELECT * FROM refresh_tokens WHERE token = ?
    DB-->>RTR: RefreshToken
    RTR-->>AS: RefreshToken
    AS->>AS: refreshToken.revoked = true
    AS->>RTR: save(refreshToken)
    RTR->>DB: UPDATE refresh_tokens SET revoked = true WHERE id = ?
    AS-->>AC: void
    AC-->>U: 204 No Content
```

---

## 2. Logout - Token Not Found (Idempotent)

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant RTR as 🗄️ RefreshTokenRepository
    participant DB as 💾 MySQL DB

    U->>AC: POST /api/auth/logout
    AC->>AS: logout(refreshToken)
    AS->>RTR: findByToken(refreshToken)
    RTR->>DB: SELECT * FROM refresh_tokens WHERE token = ?
    DB-->>RTR: null
    RTR-->>AS: null
    AS-->>AC: void (idempotent - still returns success)
    AC-->>U: 204 No Content
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
