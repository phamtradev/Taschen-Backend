# SEQ-002a: Register

> **Sequence ID:** SEQ-002a
> **Maps to:** UC-002a
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Register

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant UR as 🗄️ UserRepository
    participant VR as 🗄️ VerificationTokenRepository
    participant DB as 💾 MySQL DB
    participant ES as 📧 Email Service

    G->>AC: POST /api/auth/register
    AC->>AS: register(RegisterRequest)
    AS->>UR: findByEmail(email)
    UR->>DB: SELECT * FROM users WHERE email = ?
    DB-->>UR: null
    UR-->>AS: null
    AS->>AS: Create User entity (isActive = false)
    AS->>AS: Hash password with BCrypt
    AS->>UR: save(user)
    UR->>DB: INSERT INTO users (...)
    DB-->>UR: User (saved)
    UR-->>AS: User
    AS->>AS: Create VerificationToken (expires in 24h)
    AS->>VR: save(verificationToken)
    VR->>DB: INSERT INTO verification_tokens (...)
    DB-->>VR: VerificationToken
    AS->>ES: sendVerificationEmail(user, token)
    ES-->>G: 📧 Email sent to inbox
    AS-->>AC: RegisterResponse
    AC-->>G: 201 Created + RegisterResponse
```

---

## 2. Register - Email Exists Error

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest
    participant AC as 🔐 AuthController
    participant AS as 🔐 AuthService
    participant UR as 🗄️ UserRepository
    participant DB as 💾 MySQL DB

    G->>AC: POST /api/auth/register
    AC->>AS: register(RegisterRequest)
    AS->>UR: findByEmail(email)
    UR->>DB: SELECT * FROM users WHERE email = ?
    DB-->>UR: User (existing)
    UR-->>AS: User
    AS-->>AC: IdInvalidException("Email already exists")
    AC-->>G: 400 Bad Request
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
