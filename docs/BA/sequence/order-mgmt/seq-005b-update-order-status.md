# SEQ-005b: Update Order Status

> **Sequence ID:** SEQ-005b
> **Maps to:** UC-005b
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Update Order Status (PENDING → PROCESSING)

```mermaid
sequenceDiagram
    autonumber
    actor SE as 👤 Seller
    participant OC as 📦 OrderController
    participant OS as 📦 OrderService
    participant OR as 🗄️ OrderRepository
    participant NS as 🔔 NotificationService
    participant DB as 💾 MySQL DB

    SE->>OC: PUT /api/orders/{orderId}/status
    OC->>OS: updateOrderStatus(orderId, PROCESSING)
    OS->>DB: SELECT * FROM orders WHERE id = ?
    DB-->>OS: Order
    OS->>OS: Validate status transition (PENDING → PROCESSING)
    OS->>DB: UPDATE orders SET status = 'PROCESSING', updated_at = NOW() WHERE id = ?
    OS->>NS: notifyCustomer(orderId, "Order is being processed")
    NS-->>SE: 🔔 Notification sent
    OS-->>OC: OrderResponse
    OC-->>SE: 200 OK + OrderResponse
```

---

## 2. Update Order Status (PROCESSING → DELIVERING)

```mermaid
sequenceDiagram
    autonumber
    actor SE as 👤 Seller
    participant OC as 📦 OrderController
    participant OS as 📦 OrderService
    participant DB as 💾 MySQL DB
    participant NS as 🔔 NotificationService

    SE->>OC: PUT /api/orders/{orderId}/status
    OC->>OS: updateOrderStatus(orderId, DELIVERING)
    OS->>DB: SELECT * FROM orders WHERE id = ? AND status = 'PROCESSING'
    DB-->>OS: Order
    OS->>DB: UPDATE orders SET status = 'DELIVERING', updated_at = NOW() WHERE id = ?
    OS->>NS: notifyCustomer(orderId, "Your order has been shipped")
    NS-->>SE: 🔔 Notification sent
    OS-->>OC: OrderResponse
    OC-->>SE: 200 OK + OrderResponse
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
