# SEQ-001: Browse & Search Books

> **Sequence ID:** SEQ-001
> **Maps to:** UC-001
> **Phiên bản:** 1.0.0
> **Ngày:** 2026-04-25

---

## 1. Browse All Books (Paginated)

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books?page=1&size=10&sortBy=id&sortDir=asc
    BC->>BS: getAllBooks(Pageable)
    BS->>BR: findAll(Pageable)
    BR->>DB: SELECT * FROM books LIMIT 10 OFFSET 0
    DB-->>BR: List<Book>
    BR-->>BS: Page<Book>
    BS-->>BC: Page<Book>
    BC-->>G: 200 OK + PageResponse<BookResponse>
```

---

## 2. Get Book Detail

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant BC as 📚 BookController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>BC: GET /api/books/{bookId}
    BC->>BS: getBookById(bookId)
    BS->>BR: findById(bookId)
    BR->>DB: SELECT * FROM books WHERE id = ?
    DB-->>BR: Book (or null)
    alt Book exists
        BR-->>BS: Book
        BS-->>BC: BookResponse
        BC-->>G: 200 OK + BookResponse
    else Book not found
        BR-->>BS: null
        BS-->>BC: IdInvalidException
        BC-->>G: 400 Bad Request
    end
```

---

## 3. Get Books by Category

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant CC as 📂 CategoryController
    participant BS as 📚 BookService
    participant BR as 🗄️ BookRepository
    participant DB as 💾 MySQL DB

    G->>CC: GET /api/books/category/{categoryId}
    CC->>BS: getBooksByCategoryId(categoryId)
    BS->>BR: findByCategoryId(categoryId)
    BR->>DB: SELECT b.* FROM books b<br>JOIN book_categories bc ON b.id = bc.book_id<br>WHERE bc.category_id = ?
    DB-->>BR: List<Book>
    BR-->>BS: List<Book>
    BS-->>CC: List<BookResponse>
    CC-->>G: 200 OK + List<BookResponse>
```

---

## 4. Find Similar Books (AI)

```mermaid
sequenceDiagram
    autonumber
    actor U as 👤 User
    participant BC as 📚 BookController
    participant BES as 🤖 BookEmbeddingService
    participant BER as 🗄️ BookEmbeddingRepository
    participant DB as 💾 MySQL DB

    U->>BC: GET /api/books/{bookId}/similar?limit=10
    BC->>BES: findSimilarBooks(bookId, limit)
    BES->>BER: findByBookId(bookId)
    BER->>DB: SELECT * FROM book_embeddings WHERE book_id = ?
    DB-->>BER: BookEmbedding (vector)
    BER-->>BES: BookEmbedding
    BES->>BES: Calculate cosine similarity with other embeddings
    BES->>BER: findTopSimilar(bookId, limit)
    BER-->>BES: List<BookEmbedding>
    BES->>BES: Fetch book details for each
    BES-->>BC: List<BookResponse>
    BC-->>U: 200 OK + List<BookResponse>
```

---

## 5. Browse Categories

```mermaid
sequenceDiagram
    autonumber
    actor G as 👤 Guest/User
    participant CC as 📂 CategoryController
    participant CS as 📂 CategoryService
    participant CR as 🗄️ CategoryRepository
    participant DB as 💾 MySQL DB

    G->>CC: GET /api/categories
    CC->>CS: getAllCategories()
    CS->>CR: findAll()
    DB-->>CR: List<Category>
    CR-->>CS: List<Category>
    CS-->>CC: List<CategoryResponse>
    CC-->>G: 200 OK + List<CategoryResponse>
```

---

*Generated by Senior BA Agent | BookStore Backend | 2026-04-25*
